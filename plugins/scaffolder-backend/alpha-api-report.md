## API Report File for "@backstage/plugin-scaffolder-backend"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts
/// <reference types="node" />

import { BackendFeature } from '@backstage/backend-plugin-api';
import { JsonObject } from '@backstage/types';
import { JsonValue } from '@backstage/types';
import { Logger } from 'winston';
import { Observable } from '@backstage/types';
import { Schema } from 'jsonschema';
import { TaskSpec } from '@backstage/plugin-scaffolder-common';
import { TemplateInfo } from '@backstage/plugin-scaffolder-common';
import { UserEntity } from '@backstage/catalog-model';
import { Writable } from 'stream';

// @alpha
export const scaffolderCatalogModule: () => BackendFeature;

// @alpha
export const scaffolderPlugin: (
  options: ScaffolderPluginOptions,
) => BackendFeature;

// @alpha
export type ScaffolderPluginOptions = {
  actions?: TemplateAction<any>[];
  taskWorkers?: number;
  taskBroker?: TaskBroker;
  additionalTemplateFilters?: Record<string, TemplateFilter>;
  additionalTemplateGlobals?: Record<string, TemplateGlobal>;
};

// Warnings were encountered during analysis:
//
// src/ScaffolderPlugin.d.ts:8:5 - (ae-forgotten-export) The symbol "TemplateAction" needs to be exported by the entry point alpha.d.ts
// src/ScaffolderPlugin.d.ts:10:5 - (ae-forgotten-export) The symbol "TaskBroker" needs to be exported by the entry point alpha.d.ts
// src/ScaffolderPlugin.d.ts:11:5 - (ae-forgotten-export) The symbol "TemplateFilter" needs to be exported by the entry point alpha.d.ts
// src/ScaffolderPlugin.d.ts:12:5 - (ae-forgotten-export) The symbol "TemplateGlobal" needs to be exported by the entry point alpha.d.ts

// (No @packageDocumentation comment for this package)
```
